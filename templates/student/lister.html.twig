<h1> liste étudiant
<a href="{{path('ajouters',)}}" > add</a>
</h1>

<table border=5>
{%for c in class %}
<tr>
<td> {{c.nsc}} </td>
<td> {{c.email}}  </td>
<td> <a href="{{path('modifier',{'id':c.nsc})}}" > edit</a>

<a href="{{path('delete',{'id':c.nsc})}}" > delete</a> </td>
</tr>
{% endfor %}
</table>